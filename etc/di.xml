<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">

    <!--
        La commande entre dans la liste de `bin/magento`. C'est le seul point où
        le module touche Magento : tout le reste est du PHP ordinaire que le
        conteneur instancie sans rien savoir de Durable.
    -->
    <type name="Magento\Framework\Console\CommandListInterface">
        <arguments>
            <argument name="commands" xsi:type="array">
                <item name="durable_worker" xsi:type="object">Gplanchat\DurableModule\Console\Command\RunWorkerCommand</item>
            </argument>
        </arguments>
    </type>

    <!--
        La déclaration, faute de tags. Le conteneur de Magento n'a pas d'équivalent de
        l'autoconfiguration par attribut de Symfony : une classe de workflow se nomme, un
        gestionnaire d'activités se pose. Ce qui ne se déclare **pas**, c'est le contrat d'un
        gestionnaire — la fabrique lit ses interfaces et garde celles qui portent des
        `#[ActivityMethod]`. Une déclaration de moins est une déclaration qu'on ne peut pas écrire
        de travers, et les noms restent ceux des attributs.

        Les deux tableaux sont **vides** dans le paquet publié, et c'est délibéré : un module
        d'intégration n'a pas à livrer des workflows de démonstration que tout projet consommateur
        porterait ensuite pour rien. La démonstration vit dans le banc de ce dépôt
        (`magento/app/code/Gplanchat/DurableProbe`), qui montre exactement ces deux déclarations.

        C'est ici qu'un projet ajoute les siens :

            <item name="place_order" xsi:type="string">Acme\Shop\Workflow\PlaceOrder</item>
            <item name="order" xsi:type="object">Acme\Shop\Activity\OrderActivities</item>
    -->
    <type name="Gplanchat\DurableModule\Runtime\RuntimeFactory">
        <arguments>
            <argument name="workflowClasses" xsi:type="array">
            </argument>
            <!--
                ⚠ Un argument de constructeur **optionnel** n'est pas auto-câblé : Magento prend
                son défaut. Sans cette ligne, `deploymentConfig` reste `null`, le DSN n'est jamais
                lu, et le module sert un journal en mémoire alors qu'`env.php` demande le cluster —
                sans une ligne d'erreur nulle part. Le nullable existe pour que la fabrique reste
                construisible hors Magento, sous test unitaire ; le conteneur, lui, doit le dire.
            -->
            <argument name="deploymentConfig" xsi:type="object">Magento\Framework\App\DeploymentConfig</argument>
            <argument name="activityHandlers" xsi:type="array">
            </argument>
        </arguments>
    </type>

</config>
